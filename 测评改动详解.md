# lcah-v3 测评体系改动详解

本文档从测评层次、新增测试类型、具体测试方法和评估策略四个维度，逐层对比 lcah 与 lcah-v3 的测评体系变化。

---

## 目录

1. [概览：从单层到多层测试金字塔](#1-概览从单层到多层测试金字塔)
2. [第一层：单元测试的继承与改动](#2-第一层单元测试的继承与改动)
3. [第二层：验收测试（全新）](#3-第二层验收测试全新)
4. [第三层：架构边界测试（全新）](#4-第三层架构边界测试全新)
5. [第四层：发布烟测（全新）](#5-第四层发布烟测全新)
6. [第五层：真人场景测试（全新）](#5-第五真人场景测试全新)
7. [测试基础设施的改进](#7-测试基础设施的改进)
8. [完整测试矩阵](#8-完整测试矩阵)

---

## 1. 概览：从单层到多层测试金字塔

### lcah 测试体系

```
┌───────────────────────────┐
│      8 个单元测试文件       │
│                           │
│  test_lcah.py (995 行)    │  ← 所有功能混在一个大文件
│  test_safety_invariants   │
│  test_memory.py           │
│  test_context_manager.py  │
│  test_evaluator.py        │
│  test_run_store.py        │
│  test_skills.py           │
│  test_task_state.py       │
└───────────────────────────┘
```

特点：只有一层——单元测试。`test_lcah.py` 承担了从 agent 核心行为到 provider 客户端协议到 CLI 参数解析的全部测试，995 行塞在一个文件里。

### lcah-v3 测试体系

```
┌──────────────────────────────────────────────┐
│         真人场景测试 (50 场景)                 │  ← 端到端
│  scripts/run_v3_human_scenario_gate.py       │    真实 CLI/REPL
│  结合真实 provider 跑全链路                    │
└──────────────────────────────────────────────┘
                     ▲
┌──────────────────────────────────────────────┐
│         发布烟测 (3 测试)                     │  ← 关键路径
│  test_release_smoke.py                       │    Scripted + Live 双模式
│  读-改-写闭环 + dream 真实整合                │
└──────────────────────────────────────────────┘
                     ▲
┌──────────────────────────────────────────────┐
│         架构边界测试 (1 测试)                  │  ← 工艺保障
│  test_architecture_boundaries.py             │    文件行数熵预算
│  每个模块有硬上限，超限即失败                    │
└──────────────────────────────────────────────┘
                     ▲
┌──────────────────────────────────────────────┐
│         验收测试 (7 文件)                      │  ← 子系统级
│  test_engine_acceptance.py                   │    事件流 + 错误处理
│  test_skills_acceptance.py                   │    skill 发现/调用/事件
│  test_tool_policy_acceptance.py               │    prior_read/白名单
│  test_permissions_acceptance.py               │    plan mode 权限
│  test_context_governance_acceptance.py        │    预算+压缩+自动 compact
│  test_real_session_acceptance.py              │    Gate 8 真实 session
│  test_agent_workers_acceptance.py             │    worker 生命周期
│  test_runtime_evidence_acceptance.py           │    证据完整性
│  test_todo_ledger_acceptance.py               │    todo CRUD
└──────────────────────────────────────────────┘
                     ▲
┌──────────────────────────────────────────────┐
│         单元测试 (继承+拆分，~2000+ 行)         │  ← 原子级
│  test_lcah.py (1901 行)  — 从 995 行增长      │
│  test_safety_invariants.py — 安全不变量       │
│  test_memory.py — 记忆系统                    │
│  test_v3_runtime.py — runtime + 事件总线       │
│  test_sandbox_*.py — 沙箱配置+执行器          │
│  test_usage.py — usage/context 命令           │
│  test_ask_user.py — ask_user 工具            │
│  test_metrics.py — metrics 计算              │
│  test_run_evidence.py — evidence adapter     │
│  test_tui.py — TUI 组件                      │
│  test_run_store.py, test_task_state.py,      │
│  test_context_manager.py, test_evaluator.py   │
└──────────────────────────────────────────────┘
```

关键变化：从 8 个纯单元测试文件 → 27 个测试文件覆盖 5 层金字塔，224 个测试用例。

---

## 2. 第一层：单元测试的继承与改动

### 2.1 继承的测试

lcah 的 8 个测试文件中有 7 个在 v3 中保留了同名文件，但内容有不同程度的修改：

| lcah 测试 | lcah-v3 测试 | 改动程度 |
|-----------|-------------|---------|
| `test_lcah.py` | `test_lcah.py` | 大幅扩展（995→1901 行） |
| `test_safety_invariants.py` | `test_safety_invariants.py` | 适配 v3 API + 新增场景 |
| `test_memory.py` | `test_memory.py` | 大幅扩展（持久记忆全链路） |
| `test_context_manager.py` | `test_context_manager.py` | 适配 v3 section 模型 |
| `test_task_state.py` | `test_task_state.py` | 适配 |
| `test_run_store.py` | `test_run_store.py` | 适配 |
| `test_evaluator.py` | `test_evaluator.py` | 适配 |
| `test_skills.py` | 无（被 `test_skills_acceptance.py` 替代） | 重写为验收测试 |

### 2.2 test_lcah.py 的具体增长

lcah 的 `test_lcah.py` 覆盖：agent 工具执行、session 恢复、retry 机制、delegate 子 agent、XML 工具解析、provider 客户端协议、CLI 参数解析、budget metadata、trace/report 持久化。共 995 行。

v3 的 `test_lcah.py` 在原有基础上新增了（1901 行）：

| 新增测试 | 验证内容 |
|---------|---------|
| `test_build_agent_uses_deepseek_provider_and_env_configuration` | DeepSeek provider 的环境变量配置 |
| `test_build_agent_uses_provider_profile_protocol_from_project_toml` | 从 `.lcah.toml` 加载 provider profile |
| `test_build_agent_uses_deepseek_default_model_when_env_is_missing` | DeepSeek 默认模型兜底 |
| `test_build_arg_parser_leaves_provider_to_config_by_default` | provider 默认走 config 而非硬编码 |
| `test_agent_creates_checkpoint_when_context_reduction_happens` | checkpoint 在 context 缩减时自动创建 |
| `test_resume_prompt_uses_checkpoint_state_not_just_history` | resume 时 prompt 包含 checkpoint 状态 |
| `test_resume_invalidates_stale_file_summaries_and_marks_partial_stale` | 文件变更后 resume 检测 partial-stale |
| `test_resume_marks_workspace_mismatch_when_checkpoint_runtime_identity_is_stale` | runtime identity 不匹配检测 |
| `test_resume_marks_schema_mismatch_when_checkpoint_version_is_incompatible` | 检查点 schema 版本不兼容 |
| `test_resume_marks_no_checkpoint_when_session_has_no_checkpoint_state` | 无检查点状态 |
| `test_freshness_mismatch_creates_checkpoint_before_model_completion` | freshness 不匹配时在模型调用前创建检查点 |
| `test_runtime_identity_persists_key_execution_metadata` | runtime identity 持久化 |
| `test_resume_records_runtime_identity_mismatch_fields_in_metadata_and_trace` | identity 不匹配的字段记录 |
| `test_partial_success_creates_process_note` | partial_success 产生 process note |
| `test_write_file_trace_records_minimum_tool_contract_fields` | trace 中工具调用的合约字段 |
| `test_explicit_memory_promotion_persists_durable_memory_topics` | promotion 写入持久主题 |
| `test_final_memory_tags_are_appended_to_daily_log` | `<memory>` 标签提取到 daily log |
| `test_memory_maintenance_report_explains_auto_dream_skip_reason` | auto-dream 跳过原因记录 |
| `test_memory_maintenance_failure_does_not_mask_final_answer` | 记忆维护失败不影响最终答案 |
| `test_memory_dir_is_workspace_relative_and_repo_local` | memory_dir 路径约束 |
| `test_auto_dream_runs_in_background_after_session_gate` | auto-dream 后台执行 |
| `test_background_auto_dream_failure_restores_lock_and_reports_error` | dream 失败恢复锁 |
| `test_explicit_memory_promotion_accepts_bullet_prefixed_labels` | 带 bullet 前缀的 promotion |
| `test_explicit_memory_promotion_supports_chinese_intent_and_labels` | 中文意图和标签 |
| `test_explicit_memory_promotion_rejects_secret_shaped_and_transient_lines` | 拒绝 secret/瞬态/噪声 |
| `test_explicit_memory_promotion_supersedes_matching_durable_fact` | 相同 subject 替换 |
| `test_explicit_memory_promotion_dedupes_duplicate_durable_note` | 重复笔记去重 |
| `test_provider_error_metadata_sanitizes_url_credentials` | provider 错误 URL 脱敏 |
| `test_provider_url_sanitizer_handles_invalid_ports_and_ipv6` | URL sanitizer 边界 |
| `test_openai_compatible_client_retries_rate_limit_and_records_retry_metadata` | rate limit 重试 + metadata |
| `test_openai_compatible_client_classifies_invalid_json_provider_failure` | 无效 JSON 分类 |
| `test_provider_success_metadata_sanitizes_url_credentials` | 成功响应也脱敏 URL |

### 2.3 test_safety_invariants.py 的改动

lcah 的安全不变量测试（232 行）包含：路径逃逸拒绝、符号链接遍历拒绝、审批拒绝、secret env 配置、子 agent 深度限制。

v3 版本（212 行）的改动：
- `FakeModelClient` 替换为 `ScriptedModelClient`
- `MiniAgent` 替换为 `LCAH`
- 新增 `test_cli_build_agent_loads_project_env_secrets_before_redaction_setup`——验证 `.env` 中的 secret 在脱敏设置之前被正确加载
- 删除了 `test_bound_tool_methods_delegate_into_tools_module`（v3 的 tool 架构改为通过 `tool_executor.run_tool` 分发，不再有 delegate 工具的 bound method）

### 2.4 新增单元测试文件

| 文件 | 测试内容 |
|------|---------|
| `test_v3_runtime.py` | Engine 驱动 session、事件总线持久化、tool 事件的 session event 包装、abort 行为 |
| `test_sandbox_config.py` | 沙箱配置解析（TOML、CLI 参数合并） |
| `test_sandbox_runner.py` | 沙箱执行器（bubblewrap argv 构建、plain fallback） |
| `test_usage.py` | `/usage` 命令输出格式 |
| `test_ask_user.py` | ask_user 工具的参数校验、CLI callback 行为 |
| `test_metrics.py` | metrics 计算逻辑 |
| `test_run_evidence.py` | evidence adapter 的数据转换 |
| `test_tui.py` | TUI widget 渲染和事件处理 |

---

## 3. 第二层：验收测试（全新）

验收测试是 v3 新增的核心层，每个文件验证一个子系统在"组装后"的完整行为，而非单元级的函数正确性。

### 3.1 test_engine_acceptance.py — Engine 事件流

验证 Engine 生成器产出的完整事件序列和持久化产物的对应关系。

```python
def test_engine_streams_a_real_session_with_tool_artifacts(tmp_path):
    agent = build_agent(tmp_path, [
        '<tool name="write_file" path="notes/result.txt"><content>ok\n</content></tool>',
        "<final>Wrote it.</final>",
    ])

    events = list(agent.engine.run_turn("create the result file"))

    # 验证事件流顺序
    assert [event["type"] for event in events] == [
        "turn_started", "model_requested", "model_parsed",
        "tool_call", "tool_result",
        "model_requested", "model_parsed",
        "final", "turn_finished",
    ]

    # 验证文件切实写入
    assert (tmp_path / "notes" / "result.txt").read_text() == "ok\n"

    # 验证 session events.jsonl 持久化
    persisted_events = read_jsonl(agent.session_event_bus.path)
    assert [event["event"] for event in persisted_events][-6:] == [
        "tool_finished", "context_usage_recorded", "model_requested",
        "model_parsed", "assistant_message", "turn_finished",
    ]

    # 验证 report.json
    report = json.loads((agent.current_run_dir / "report.json").read_text())
    assert report["status"] == "completed"
```

还验证了 provider error 场景：`ProviderError("rate_limited")` 被 engine 正确处理为 `status="failed"`、`stop_reason="model_error"`，且中文错误消息包含 `"模型错误"`。

### 3.2 test_skills_acceptance.py — Skills 子系统

验证从发现到执行的完整链路：

```python
def test_builtin_skills_are_available_in_context(tmp_path):
    # 验证 4 个内置 skill 存在且出现在 prompt 中
    names = [skill.name for skill in skillslib.list_skills(agent.skills)]
    assert {"review", "test", "commit", "simplify"}.issubset(names)
    # 验证 prompt 中 skills section 的位置：memory 后、relevant_memory 前
    assert prompt.index("Memory:") < prompt.index("Available skills:") < prompt.index("Relevant memory:")

def test_prompt_includes_auto_memory_policy_and_index(tmp_path):
    # 验证 Auto Memory section 被注入 prompt
    # 验证 MEMORY.md 内容出现在 prompt 中

def test_prompt_documents_project_skill_frontmatter_contract(tmp_path):
    # 验证 SKILL_FILE_CREATION_GUIDE 在 prompt 中

def test_project_skill_slash_invocation_runs_inline_session(tmp_path):
    # 创建项目 skill 文件 → 用 handle_repl_command 模拟 /deploy 调用
    # 验证 handled=True, skill prompt 正确展开 $ARGUMENTS 和 ${LCAH_SKILL_DIR}
    # 验证 session events 中包含 "skill_invoked" 事件
```

### 3.3 test_tool_policy_acceptance.py — 工具策略

验证 prior_read_required 和 self_authored_file_freshness 两条策略：

```python
def test_patch_requires_prior_fresh_read_and_allows_after_read(tmp_path):
    # 1. 未 read 直接 patch → 拒绝，错误码 prior_read_required
    rejected = agent.run_tool("patch_file", ...)
    assert agent._last_tool_result_metadata["tool_error_code"] == "prior_read_required"

    # 2. 先 read 再 patch → 通过
    agent.run_tool("read_file", ...)
    assert agent.run_tool("patch_file", ...) == "patched README.md"

def test_write_file_allows_new_file_but_requires_read_before_overwrite(tmp_path):
    # 新文件 → 允许，已有文件 → 需要 prior read

def test_patch_allows_self_authored_file_without_extra_read(tmp_path):
    # agent 自己写的文件 → 不需要额外的 read 就能 patch

def test_rejected_patch_can_be_retried_after_informing_read(tmp_path):
    # 模拟完整 ask：被拒 → 读文件 → 再 patch → 成功
    # 验证 trace 中有两次 patch 事件，第一次 error_code="prior_read_required"，第二次为空
```

### 3.4 test_permissions_acceptance.py — 权限门控

验证 plan mode 下的工具限制和 permission gate。

### 3.5 test_context_governance_acceptance.py — 上下文治理

验证 prompt 预算管理、section 压缩、自动 compact 触发。

### 3.6 test_real_session_acceptance.py — Gate 8 真实 Session

这叫"Gate 8"是因为它验证 8 个真实 session 场景的端到端行为。测试动态加载 `scripts/run_real_session_acceptance.py` 并验证其输出。

```python
def test_gate8_acceptance_harness_writes_real_session_evidence_bundle(tmp_path):
    run_acceptance = _load_run_acceptance()
    summary = run_acceptance(output_dir)

    assert summary["status"] == "passed"
    assert summary["scenario_count"] >= 9

    # 验证 9 个场景 ID 全部覆盖
    scenario_ids = {s["id"] for s in summary["scenarios"]}
    assert {"bugfix_pytest", "plan_todo_explore", "skill_inline", "worker_write_scope",
            "resume_continuation", "security_rejection", "context_pressure",
            "provider_error_recovery", "live_provider_smoke"}.issubset(scenario_ids)

    # 验证 bugfix_pytest 场景：源码被正确修复
    bugfix_report = json.loads(...)
    assert "src/calculator.py" in bugfix_report["task_state"]["changed_paths"]
    # 验证 calculator.py 确实变成了正确的代码
    assert content == "def add(a, b):\n    return a + b\n"

    # 验证 plan_todo_explore 场景：todo 完成 + worker 类型正确
    assert plan_report["todos"]["items"][0]["status"] == "done"
    assert plan_report["workers"]["items"][0]["subagent_type"] == "Explore"

    # 验证 worker_write_scope 场景：文件只写到允许的路径
    content1 = (output_dir / worker["workspace_relpath"] / "notes" / "first.txt")
    assert content1 == "first\n"
```

每个场景的验证标准：
- `report.json` 的 `status` 和 `stop_reason` 正确
- `trace.jsonl` 存在
- `session_event_path` 存在且含 `turn_finished` 事件
- 文件系统的副作用符合预期（如文件被正确修改、worker 只在 write_scope 内产出文件）

### 3.7 test_agent_workers_acceptance.py — Worker 生命周期

验证子 agent 的创建、状态跟踪、通知推送。

### 3.8 test_runtime_evidence_acceptance.py — 证据完整性

验证每次 run 的 trace、report、task_state 之间的交叉引用一致性（如 `report.run_id == task_state.run_id`、`checkpoint_id` 在 task_state、report、trace 中一致）。

### 3.9 test_todo_ledger_acceptance.py — Todo CRUD

验证 todo_add、todo_update、todo_list 工具的行为和 todo_changes 记录。

---

## 4. 第三层：架构边界测试（全新）

`test_architecture_boundaries.py` 是一个**工艺保障测试**，不测功能正确性，只测代码结构。

```python
def test_core_modules_stay_below_entropy_budget():
    root = Path(__file__).resolve().parents[1]
    budgets = {
        "lcah/core/runtime.py": 950,
        "lcah/core/engine.py": 470,
        "lcah/core/context_manager.py": 420,
        "lcah/features/skills.py": 220,
        "lcah/tools/registry.py": 360,
        # ... 24 个模块的硬上限
    }

    for relative_path, max_lines in budgets.items():
        line_count = len((root / relative_path).read_text().splitlines())
        assert line_count <= max_lines, f"{relative_path} has {line_count} lines, budget is {max_lines}"
```

**为什么存在**：防止模块因持续加功能而膨胀到不可维护。如果一个模块的行数超过了它的"熵预算"，测试直接失败——这不是 bug，是提醒你需要做拆分或重构。

**实际作用**：在开发过程中，这是**自动化的代码审查**。如果有人往 `engine.py` 塞了太多逻辑，CI 直接红。

---

## 5. 第四层：发布烟测

`test_release_smoke.py` 验证在发布前必须通过的两个关键用户旅程。

### 5.1 Scripted 模式（CI 始终运行）

```python
def test_read_then_edit_completes_in_one_turn(tmp_path):
    """模型读 README → 改一行 → 完成。验证读取-编辑闭环。"""
    agent = _build_agent(tmp_path, [
        '<tool>{"name":"read_file","args":{"path":"README.md","start":1,"end":20}}</tool>',
        '<tool>{"name":"patch_file","args":{...}}</tool>',
        "<final>已把 Quick Start 段从占位文本改成实际的 lcah --help 提示。</final>",
    ])
    final = agent.engine.ask("把 README 里的 Quick Start 段改成实际命令")
    assert "Quick Start" in final
    updated = (tmp_path / "README.md").read_text()
    assert "lcah --help" in updated

def test_search_then_summarize_succeeds(tmp_path):
    """模型 search 找 TODO 标记 → 汇报。验证只读探索流程。"""

def test_step_limit_default_can_handle_realistic_workflows(tmp_path):
    """默认 max_steps=50 能扛住 7 步 read 流程"""

def test_empty_response_does_not_silently_stop(tmp_path):
    """ProviderError("empty_response") 必须中文用户可见，不能静默静默停止"""
```

### 5.2 Live 模式（真实 provider）

```python
@pytest.mark.skipif(not _has_live_provider(), reason="set LCAH_LIVE_SMOKE=1")
def test_dream_produces_non_empty_topics_with_live_provider(tmp_path):
    """End-to-end: 真实 provider 跑一次 dream，topics/ 必须产出非空文件。"""
    # 准备：写 3 条 daily log（pytest、make test、API key 规则）
    log_path.write_text(
        "- 测试项目使用 pytest 而非 unittest\n"
        "- 部署前必须运行 `make test` 和 `make lint`\n"
        "- 切勿提交真实的 API key\n"
    )

    # 用真实 provider 配置创建 agent，调用 run_dream()
    agent.run_dream()

    # 验证：topics/ 下至少有一个非空 .md 文件
    topics_dir = tmp_path / ".lcah" / "memory" / "topics"
    written = [p for p in topics_dir.glob("*.md") if p.read_text().strip()]
    assert written, "dream 必须至少产出一个非空 topic 文件"

    # 验证：MEMORY.md 索引包含 topics/ 引用
    index = (tmp_path / ".lcah" / "memory" / "MEMORY.md").read_text()
    assert "topics/" in index
```

**运行方式**：
```bash
# CI: ScriptedModelClient 模式（无需 API key）
pytest tests/test_release_smoke.py -q

# 真实 provider 模式
LCAH_LIVE_SMOKE=1 pytest tests/test_release_smoke.py::test_dream_produces_non_empty_topics_with_live_provider -q
```

---

## 6. 第五层：真人场景测试（全新）

这是 lcah-v3 测评体系最独特的一层。它不是代码测试，而是**像真实用户一样用 lcah 完成任务，然后审计产物**。

### 6.1 设计理念

```
不是测"这个函数对不对"，
而是测"一个用户打开终端，敲 uv run lcah，能不能完成一个真实开发任务"。

不是 mock 外部依赖然后在内存中跑，
而是真实地启 CLI/REPL，真实调 provider，真实读产物文件做断言。
```

### 6.2 场景设计（50 个场景）

`release/v3/testing/01-test-design.md` 定义了 50 个场景，按改动面分组：

| 分组 | 示例场景 |
|------|---------|
| Runtime & CLI | S01 启动 TUI、S02 启动 REPL、S03 one-shot、S04 指定 provider、S05 指定 approval |
| REPL & Slash | S06-S10: /help、/session、/history、/resume、/clear |
| Plan Mode | S11-S14: /plan 进入、read-only 工具、/plan-exit、plan 文件产出 |
| Tool Policy | S15-S18: read-before-write、重复调用拒绝、路径逃逸 |
| Skills | S19-S22: /review、/test、用户自定义 skill、skill fork |
| Subagent | S23-S26: Explore agent、Worker agent、write_scope、send_message |
| Memory | S27-S35: /remember、/memory、/working-memory、/dream、auto-dream、durable promotion、中文记忆 |
| Provider | S36-S40: provider profile、key fallback、错误恢复、错误 URL 脱敏 |
| Session & Evidence | S41-S45: resume、freshness mismatch、workspace mismatch、compact、report |
| 安全 & 边界 | S46-S50: secret 脱敏、沙箱、long-path workspace、中文 prompt、超长 session |

### 6.3 执行方式

```bash
# 跑 12 个优先 gate 场景
uv run python scripts/run_v3_human_scenario_gate.py

# 跑完整 50 场景
uv run python scripts/run_v3_human_scenario_gate.py --suite full

# 只跑指定场景
uv run python scripts/run_v3_human_scenario_gate.py --suite full --scenario S21
```

### 6.4 Runner 原理

`scripts/run_v3_human_scenario_gate.py` 的工作方式：

```
对每个场景：
  1. 创建一个隔离的临时工作区（含项目文件）
  2. 用 subprocess 启动真正的 lcah CLI（uv run lcah --cwd <tmp> ...）
  3. 通过 stdin 发送用户输入序列（模拟用户打字）
  4. 等待进程结束
  5. 读取 .lcah/runs/<run_id>/report.json
  6. 读取 .lcah/runs/<run_id>/trace.jsonl
  7. 读取 .lcah/sessions/<id>.events.jsonl
  8. 验证关键断言（report status、文件变更、事件序列）
```

### 6.5 关键原则

- 场景必须从用户入口驱动——通过 `subprocess` 调用 `lcah` CLI，不能 `import LCAH` 直接调 runtime
- 验证器只能读 lcah 自己写出的 artifacts，不能读内部内存状态
- 输出目录必须在 repo 外，避免 lcah 向上发现真实 repo root
- live provider 默认用 DeepSeek，key 来自项目 `.lcah.toml`，不写入文档或产物
- 发现产品问题时先修产品本身，再补 narrow regression test；不能通过放宽场景断言来掩盖

### 6.6 最终结果

最后一次干净全量结果（2026-05-13）：

```text
uv run python scripts/run_v3_human_scenario_gate.py --suite full
{"failed": 0, "output_dir": "...", "passed": 50, "status": "passed"}

uv run pytest tests -q
224 passed, 2 skipped, 6 warnings in 68.50s
```

---

## 7. 测试基础设施的改进

### 7.1 ScriptedModelClient（替代 FakeModelClient）

lcah 使用 `FakeModelClient(outputs)`：一个简单的列表，按顺序弹出字符串作为模型输出。

lcah-v3 使用 `ScriptedModelClient(outputs)`：从 `lcah.testing` 导出，支持两种 scripted output：

```python
# 字符串：模型返回该文本
ScriptedModelClient(["<final>Done.</final>"])

# ProviderError：模型抛出该异常（用于测试错误恢复路径）
ScriptedModelClient([ProviderError("rate_limited", ...)])
```

`ScriptedModelClient` 不在公开 API 中导出（`test_public_api_exports_resolve_through_package_path` 显式验证了 `not hasattr(lcah_pkg, "ScriptedModelClient")`），它只存在于 `lcah.testing`，表示它是测试专用工具而非公共 API。

### 7.2 临时工作区模式

所有测试使用 `tmp_path`（pytest fixture）作为隔离工作区，在内存或临时目录中创建。这确保了：

- 测试互不干扰
- 不需要手动清理
- 可以在 CI 中并行运行

### 7.3 Event-driven 验证

v3 测试大量验证 session events 和 trace events 的完整性：

```python
def read_session_events(agent):
    path = agent.session_event_bus.path
    return [json.loads(line) for line in path.read_text().splitlines() if line.strip()]

# 验证事件序列
assert event_names(agent) == [
    "session_started", "turn_started", "user_message",
    "context_usage_recorded", "model_requested", "model_parsed",
    "assistant_message", "turn_finished",
]
```

### 7.4 pytest 配置

lcah-v3 的 `pyproject.toml` 新增 pytest 配置：

```toml
[tool.pytest.ini_options]
norecursedirs = [".git", ".venv", "artifacts", "build", "dist"]
```

排除 artifacts/build/dist 目录能避免 pytest 在这些目录中搜索测试文件，加快收集速度。

---

## 8. 完整测试矩阵

| 测试层次 | lcah | lcah-v3 | v3 新增 |
|---------|------|---------|---------|
| **单元测试** | 8 文件 | 15 文件 | +7 |
| **验收测试** | 0 | 9 文件 | +9 |
| **架构边界** | 0 | 1 文件 | +1 |
| **发布烟测** | 0 | 1 文件（6 测试） | +1 |
| **真人场景** | 0 | 50 场景 + runner | +全套 |
| **总测试用例** | ~40 | 224（pytest）+ 50（场景） | |
| **CI 运行时间** | 未测量 | ~68s（224 用例） | |
| **Live provider** | 无 | `LCAH_LIVE_SMOKE=1` 可选 | + |

### 测试覆盖的 v3 新增功能映射

| v3 新增功能 | 覆盖测试 |
|------------|---------|
| Auto-Dream | `test_lcah.py`（5 个测试） + `test_release_smoke.py`（live dream） + 场景 S32-S35 |
| Checkpoint / Resume | `test_lcah.py`（8 个测试） + 场景 S41-S45 |
| Durable Promotion | `test_lcah.py`（7 个测试） + 场景 S33 |
| Engine 事件流 | `test_engine_acceptance.py` + `test_v3_runtime.py` |
| Session Events | `test_v3_runtime.py` + 所有验收测试 |
| Tool Policy | `test_tool_policy_acceptance.py`（5 个测试） + 场景 S15-S18 |
| Skills | `test_skills_acceptance.py` + 场景 S19-S22 |
| Workers | `test_agent_workers_acceptance.py` + 场景 S23-S26 |
| Plan Mode | `test_permissions_acceptance.py` + 场景 S11-S14 |
| Provider Error | `test_engine_acceptance.py` + `test_lcah.py`（3 个测试） + 场景 S38-S40 |
| Provider Profile | `test_lcah.py`（4 个测试） + 场景 S36-S37 |
| Sandbox | `test_sandbox_config.py` + `test_sandbox_runner.py` |
| Context Governance | `test_context_governance_acceptance.py` + `test_lcah.py`（budget metadata） |
| Todo Ledger | `test_todo_ledger_acceptance.py` + 场景 S12 |
| Architecture | `test_architecture_boundaries.py` |
| Security | `test_safety_invariants.py` + 场景 S46-S50 |

---

## 总结

lcah-v3 的测评体系从 lcah 的"单一单元测试层"升级为"五层金字塔"：

1. **单元测试**：继承并扩展，覆盖所有基础逻辑单元
2. **验收测试**：每个子系统一个文件，验证组装后的完整行为，事件流一一对照
3. **架构边界**：行数熵预算，防止模块膨胀
4. **发布烟测**：Scripted + Live 双模式，验证关键用户旅程在真实 provider 下可工作
5. **真人场景**：50 个真实使用场景，从 CLI 入口驱动，从 artifacts 审计结果

这五层的设计对应了不同的失效模式和检测窗口——单元测试在函数级发现问题、验收测试在子系统级、烟测在真实调用级、场景测试在产品体验级、架构边界在开发工艺级。每一层的盲目区域恰好由下一层覆盖。
