# lcah vs lcah-v3 区别文档

本文档详细对比 `lcah` (v0.1.0) 与 `lcah-v3` (v0.3.0) 之间的差异，说明 lcah-v3 在 lcah 的基础上新增、重构和增强了哪些内容。

---

## 目录

1. [概览：规模对比](#1-概览规模对比)
2. [架构重构：从单体到分层模块化](#2-架构重构从单体到分层模块化)
3. [Provider 体系：从简单选择到 Profile 配置](#3-provider-体系从简单选择到-profile-配置)
4. [TUI 终端界面](#4-tui-终端界面)
5. [配置系统：多层级 TOML 配置](#5-配置系统多层级-toml-配置)
6. [工具系统：从 7 个到 13 个工具](#6-工具系统从-7-个到-13-个工具)
7. [子 Agent 系统（Worker 管理）](#7-子-agent-系统worker-管理)
8. [Plan Mode 计划模式](#8-plan-mode-计划模式)
9. [记忆系统：从工作记忆到持久记忆](#9-记忆系统从工作记忆到持久记忆)
10. [Skills 系统：从触发匹配到完整技能框架](#10-skills-系统从触发匹配到完整技能框架)
11. [Sandbox 沙箱隔离](#11-sandbox-沙箱隔离)
12. [Session 系统增强](#12-session-系统增强)
13. [Engine 控制循环分离](#13-engine-控制循环分离)
14. [上下文管理与压缩](#14-上下文管理与压缩)
15. [权限系统增强](#15-权限系统增强)
16. [Checkpoint 检查点与恢复](#16-checkpoint-检查点与恢复)
17. [Todo 待办系统](#17-todo-待办系统)
18. [CLI 命令行增强](#18-cli-命令行增强)
19. [测试体系](#19-测试体系)
20. [文档体系](#20-文档体系)
21. [其他差异](#21-其他差异)

---

## 1. 概览：规模对比

| 维度 | lcah | lcah-v3 |
|------|------|---------|
| Python 源文件 | 15 个 | 69 个 |
| 测试文件 | 8 个 | 29 个 |
| 代码组织 | 扁平单层 `lcah/` | 分层子包 `lcah/core/`, `lcah/tools/`, `lcah/features/`, `lcah/providers/`, `lcah/config/`, `lcah/tui/`, `lcah/evaluation/`, `lcah/commands/` |
| 版本号 | 0.1.0 | 0.3.0 |
| 外部依赖 | 无运行时依赖 | `textual>=0.86.0`（TUI）, `tomli>=2.0.0`（TOML 解析） |
| 默认 max_steps | 6 | 50 |
| 默认 max_new_tokens | 512 | 8192 |

---

## 2. 架构重构：从单体到分层模块化

### lcah（原版）
所有代码放在 `lcah/` 目录的 15 个平铺文件中：
```
lcah/
├── cli.py              # CLI + REPL + 启动装配
├── runtime.py           # 核心运行时 + 控制循环 + 模型解析 + SessionStore
├── models.py            # 模型客户端
├── tools.py             # 工具定义 + 执行 + 校验
├── context_manager.py   # 上下文管理
├── memory.py            # 工作记忆
├── skills.py            # Skills 加载与匹配
├── task_state.py        # 任务状态
├── run_store.py         # 运行持久化
├── evaluator.py         # 评估
├── metrics.py           # 指标
└── workspace.py         # 工作区快照
```

### lcah-v3（重构后）
分为 8 个子包，职责清晰分离：

```
lcah/
├── cli.py                    # CLI 入口 + REPL 命令路由
├── __main__.py
├── testing.py                # 测试辅助
├── config/                   # 配置层
│   └── __init__.py           # TOML/Env 解析、Provider Profile 合并
├── core/                     # 核心运行时层
│   ├── runtime.py            # LCAH 运行时（状态持有 + 持久化）
│   ├── engine.py             # 控制循环（生成器模式）
│   ├── engine_helpers.py     # 引擎辅助
│   ├── context_manager.py    # 上下文组装
│   ├── context_usage.py      # 上下文用量跟踪
│   ├── compact.py            # 对话压缩
│   ├── turn_history.py       # 轮次历史构建
│   ├── session_store.py      # 会话持久化
│   ├── session_events.py     # 会话事件总线
│   ├── session_lifecycle.py  # 会话生命周期管理
│   ├── task_state.py         # 任务状态
│   ├── run_store.py          # 运行证据持久化
│   ├── plan_mode.py          # 计划模式策略
│   ├── permissions.py        # 权限检查
│   ├── tool_executor.py      # 工具执行流水线
│   ├── tool_policy.py        # 工具策略（如 prior_read）
│   ├── tool_profiles.py      # 工具 Profile（default/plan/readonly）
│   ├── tool_repetition.py    # 重复调用检测
│   ├── model_output.py       # 模型输出解析
│   ├── model_errors.py       # 模型错误处理
│   ├── workspace.py          # 工作区快照
│   ├── artifacts.py          # 运行工件
│   ├── runtime_events.py     # 运行时事件构建
│   ├── runtime_consumers.py  # 运行时事件消费者
│   ├── runtime_secrets.py    # 敏感信息处理
│   ├── runtime_checkpoints.py # 检查点管理
│   ├── worker_manager.py     # Worker 生命周期管理
│   ├── worker_execution.py   # Worker 执行
│   ├── worker_runtime.py     # Worker 运行时
│   ├── worker_artifacts.py   # Worker 工件
│   ├── worker_notifications.py # Worker 通知
│   └── todo_ledger.py        # Todo 台账
├── tools/                    # 工具层
│   ├── registry.py           # 工具注册 + 基础工具执行
│   ├── base.py               # RegisteredTool 类型
│   ├── agents.py             # agent/send_message/task_stop 工具
│   ├── ask_user.py           # ask_user 工具
│   ├── plan.py               # enter_plan_mode/exit_plan_mode 工具
│   └── todos.py              # todo_add/todo_update/todo_list 工具
├── features/                 # 功能层
│   ├── memory.py             # 工作记忆 + 持久记忆 + Dream 整合
│   ├── skills.py             # Skills 发现、解析、渲染
│   ├── skills_bundled.py     # 内置 Skills
│   ├── skills_runtime.py     # Skills 运行时调用
│   └── sandbox/              # 沙箱子系统
│       ├── config.py         # 沙箱配置
│       ├── checker.py        # 沙箱可用性检查
│       ├── command_matcher.py # 命令排除匹配
│       └── runner.py         # 沙箱执行器
├── providers/                # 模型 Provider 层
│   ├── base.py               # Provider 基类 + complete_model
│   ├── clients.py            # OpenAI / Anthropic 兼容客户端
│   └── errors.py             # Provider 错误类型
├── tui/                      # 终端 UI 层
│   ├── app.py                # Textual App
│   ├── widgets.py            # UI 组件
│   └── main.py               # TUI 入口
├── commands/                 # 命令层
│   └── slash.py              # Slash 命令解析与路由
└── evaluation/               # 评估层
    ├── evaluator.py          # 评估器
    ├── metrics.py            # 指标
    └── run_evidence.py       # 运行证据
```

---

## 3. Provider 体系：从简单选择到 Profile 配置

### lcah
- 3 种 provider 硬编码：`ollama`、`openai`、`anthropic`
- 模型选择逻辑内联在 CLI 中
- API Key 直接从环境变量读取

### lcah-v3
- **Provider Profile 概念**：profile = protocol + api_key + base_url + model
- 支持通过 TOML 配置多个 provider profile（如 `deepseek`、`openai`、`anthropic`）
- Profile 的 `protocol` 决定请求格式（`openai` 或 `anthropic`），profile 名只是标签
- 配置优先级：`CLI 参数 > 环境变量 > 项目 .lcah.toml > 全局 ~/.config/lcah/config.toml > 代码默认值`
- 移除 Ollama 支持，新增 DeepSeek profile 支持
- 新增 `LCAH_API_KEY`、`LCAH_BASE_URL`、`LCAH_MODEL` 通用覆盖变量
- Provider 客户端的 `complete()` 调用统一走 `complete_model()` 函数

---

## 4. TUI 终端界面

### lcah
只有简单的 REPL（Read-Eval-Print Loop）交互模式。

### lcah-v3
新增基于 **Textual** 框架的完整 TUI：
- 默认启动 TUI（检测到终端时）
- 包含组件：`ChatLog`（对话日志）、`InputBar`（输入框）、`StatusBar`（状态栏）、`ThinkingIndicator`（思考指示器）、`ToolCard`（工具卡片）、`WelcomeBanner`（欢迎横幅）、`AskUserPrompt`（用户询问弹窗）、`ConfirmPrompt`（确认弹窗）
- 键盘绑定：`Ctrl+L` 清屏、`Ctrl+Q` 退出
- TUI 是纯展示层，底层复用同一个 `Engine.run_turn()` 生成器
- 可通过 `--repl` 强制使用传统 REPL，`--tui` 强制使用 TUI

---

## 5. 配置系统：多层级 TOML 配置

### lcah
没有配置文件支持，一切通过 CLI 参数和环境变量。

### lcah-v3
新增完整的多层级配置系统：
- **项目级**：`.lcah.toml`（位于仓库根目录）
- **全局级**：`~/.config/lcah/config.toml`
- **环境变量**：`LCAH_PROVIDER`、`<PROVIDER>_API_KEY`、`<PROVIDER>_BASE_URL`、`<PROVIDER>_MODEL`
- **CLI 参数**：`--config` 可指定自定义配置文件路径
- TOML 中可定义多个 `[providers.<name>]` 配置块
- 支持 `.env` 文件加载（`load_project_env`）
- 沙箱配置也可以从 TOML 读取

---

## 6. 工具系统：从 7 个到 13 个工具

### lcah
基础工具（7 个）：
- `list_files`、`read_file`、`search`、`run_shell`、`write_file`、`patch_file`、`delegate`

### lcah-v3
工具扩展为 13 个，并引入 `RegisteredTool` 类型：

| 工具 | 说明 | v3 新增 |
|------|------|---------|
| `list_files` | 列出文件 | - |
| `read_file` | 读取文件 | - |
| `search` | 搜索 | - |
| `run_shell` | 执行命令 | 支持沙箱执行 |
| `write_file` | 写文件 | 新增 prior_read 策略 |
| `patch_file` | 补丁修改 | 新增 prior_read 策略 |
| `agent` | 启动子 agent（取代 delegate） | **新增** |
| `send_message` | 向已有 worker 发消息 | **新增** |
| `task_stop` | 停止 worker | **新增** |
| `enter_plan_mode` | 进入计划模式 | **新增** |
| `exit_plan_mode` | 退出计划模式 | **新增** |
| `ask_user` | 向用户提问 | **新增** |
| `todo_add` / `todo_update` / `todo_list` | 待办管理 | **新增** |

新增工具策略（`ToolPolicy`）：
- `prior_read_required`：写文件/补丁前要求模型先读过该文件
- `write_scope`：worker 只能在指定路径范围内写入

---

## 7. 子 Agent 系统（Worker 管理）

### lcah
简单的 `delegate` 工具：创建一个只读的子 `LCAH` 实例，步数更少，只返回结论文本。

### lcah-v3
完整的子 agent 管理体系：

- **WorkerManager**：管理子 agent 的生命周期（创建、状态跟踪、通知、停止）
- **两种子 agent 类型**：
  - `Explore`：只读探索子 agent
  - `worker`：可写 worker（需指定 `write_scope`）
- **后台执行**：worker 可以在后台运行，支持通过 `send_message` 继续、`task_stop` 停止
- **Worker 通知**：后台 worker 完成后通过通知系统将结果反馈到会话
- **Worker 工件**：`worker_artifacts.py` 跟踪 worker 产生的文件变更
- **命令支持**：`/agents` 查看子 agent 状态，`/subagent` 手动启动

---

## 8. Plan Mode 计划模式

### lcah
没有计划模式。

### lcah-v3
新增完整的 Plan Mode：

- **进入**：`/plan <topic>` 或模型调用 `enter_plan_mode` 工具
- **Plan 文件**：自动生成到工作区的 plan 文件
- **工具限制**：plan 模式下自动切换到 `plan` tool profile，只允许只读工具
- **退出**：模型调用 `exit_plan_mode` 工具或 `/plan-exit` 命令
- **Plan 模式提示**：在 system prompt 中注入 plan 模式的特定指导
- **`/mode` 命令**：查看当前运行模式

---

## 9. 记忆系统：从工作记忆到持久记忆

### lcah
单层工作记忆（Working Memory）：
- 任务摘要
- 最近接触的文件
- 文件短摘要（file_summaries）
- 跨轮笔记（episodic_notes）
- 简单的关键词召回

### lcah-v3
三层记忆体系 + 自动整合：

**第一层 — Working Memory（工作记忆）**
- 与 lcah 原版兼容的即时记忆
- 任务摘要、最近文件、文件摘要、笔记
- Token 化关键词召回

**第二层 — Daily Logs（每日日志）**
- 通过 `/remember <text>` 手动记录
- 按日期组织：`.lcah/memory/logs/YYYY/MM/YYYY-MM-DD.md`
- 自动关键词提取和分类

**第三层 — Durable Topics（持久主题）**
- 通过 `/dream` 命令从 daily logs 中整合
- 按主题组织：`.lcah/memory/topics/*.md`
- 4 种默认分类：project-conventions、key-decisions、dependency-facts、user-preferences

**Auto-Dream（自动整合）**
- 每 N 小时（默认 24h）且累计 N 个新 session（默认 5）后自动触发
- 通过 `--no-auto-dream` 关闭
- `--dream-interval` 和 `--dream-min-sessions` 可配置
- 后台线程执行，不阻塞主交互
- Dream agent 使用独立的 LCAH 子实例执行

**Memory 索引**
- `.lcah/memory/MEMORY.md`：所有持久主题的索引文件
- `/memory` 命令查看索引
- `/working-memory` 查看当前工作记忆

---

## 10. Skills 系统：从触发匹配到完整技能框架

### lcah
简单的触发词匹配系统：
- 从 `.lcah/skills/*.md` 加载
- 前端 YAML frontmatter（name, description, triggers）
- 在用户输入中匹配 trigger 关键词
- 匹配到的 skill body 直接注入 prompt

### lcah-v3
完整的技能框架：

- **多来源发现**：
  - 内置 Skills（`skills_bundled.py`）
  - 用户 Skills（`~/.lcah/skills/<name>/SKILL.md`）
  - 项目 Skills（`skills/<name>/SKILL.md` 或 `.lcah/skills/<name>/SKILL.md`）

- **丰富的元数据**：user_invocable、disable_model_invocation、allowed_tools、model、argument_hint、paths、when_to_use、context

- **Slash Command 调用**：`/<name> [args]` 格式直接调用

- **变量替换**：`$ARGUMENTS`、`${LCAH_SKILL_DIR}`

- **Prompt 区段渲染**：按 `source` 排序后渲染到 prompt

- **命令系统支持**：
  - `/skills` 列出所有可用 skills
  - `/skill <name> [args]` 手动调用
  - `parse_slash_command()` 解析 slash 命令
  - `resolve_command()` 路由命令

---

## 11. Sandbox 沙箱隔离

### lcah
`run_shell` 直接在本地 shell 执行命令。

### lcah-v3
新增可选沙箱隔离层：

- **三种模式**：
  - `off`：不使用沙箱
  - `best_effort`：有沙箱时使用，没有时回退到直接执行
  - `required`：强制使用沙箱，不可用时拒绝执行

- **后端**：`auto`（自动检测）、`bubblewrap`（Linux bubblewrap）、`none`

- **命令排除**：可配置某些命令不走沙箱

- **文件系统边界**：沙箱内限制对工作区外路径的访问

- **SandboxRunner**：封装了沙箱执行逻辑，失败时回退或报错

- CLI 参数：`--sandbox`、`--sandbox-backend`

---

## 12. Session 系统增强

### lcah
- 简单的 JSON 文件存储会话
- `SessionStore` 只做基本保存/加载/查找

### lcah-v3
全面的会话管理：

- **SessionEventBus**：事件驱动的会话事件流
  - 事件写入 `.lcah/sessions/<id>.events.jsonl`
  - 事件类型：session_started、turn_started、user_message、model_requested、model_parsed、tool_executed、assistant_message、turn_finished、context_usage_recorded、model_retry_scheduled、worker_notification_drained、memory_note_appended、runtime_mode_changed 等
  - 事件脱敏处理

- **会话列表**：`/history` 查看历史会话
- **会话恢复**：`/resume <id|latest|index>` 续接会话
- **会话清空**：`/clear` 开新会话
- **Session Lifecycle**：`session_lifecycle.py` 管理 resume/clear 流程
- **Runtime Identity**：记录每次运行的环境指纹，用于检测跨 session 的不兼容变更

---

## 13. Engine 控制循环分离

### lcah
`ask()` 方法直接实现在 `LCAH` 类中，控制循环与状态管理耦合在一起。

### lcah-v3
`Engine` 类独立于 `LCAH` runtime：

- **生成器模式**：`run_turn(user_message)` 是生成器函数，每步 yield 事件
  - 事件类型：turn_started、model_requested、model_parsed、retry、runtime_notice、worker_notification、final、turn_finished
- **错误处理**：模型调用异常分类处理（可重试 vs 致命错误）
- **Abort 支持**：`abort_current_turn()` 可中断当前轮次
- **多工具调用**：支持一次返回多个工具调用（`kind == "tools"`）
- **Worker 通知排空**：每轮循环自动拉取并注入 worker 通知

---

## 14. 上下文管理与压缩

### lcah
基础的 `ContextManager`，负责组装 prompt。

### lcah-v3
增强的上下文管理：

- **CompactManager**：对话历史压缩
  - `/compact` 命令手动触发
  - 自动压缩：当 prompt 超出预算时自动触发（`auto_prompt_over_budget`）
  - 保留最近的 N 轮完整信息

- **ContextUsage**：上下文用量跟踪
  - `/context` 查看 prompt 上下文使用情况
  - `/usage` 查看 provider、model、token 元数据

- **Turn History**：`TurnHistoryBuilder` 丰富历史条目（添加 turn_id 等元数据）

---

## 15. 权限系统增强

### lcah
简单的审批策略（ask/auto/never）+ read_only 标志。

### lcah-v3
多维度权限控制：

- **PermissionChecker**：集中权限检查
- **Tool Profiles**：3 种内置 profile
  - `default`：所有工具可用，高风险需审批
  - `plan`：只读工具 + plan 工具
  - `readonly`：只读工具
- **ToolPolicy**：工具级策略
  - `prior_read_required`：写操作前必须读过目标文件
  - 通过 `self_authored_file_freshness` 跟踪文件 freshness，减少误拒
- **write_scope**：Worker 只能在声明范围内写入
- **Sandbox 集成**：高危命令可通过沙箱隔离

---

## 16. Checkpoint 检查点与恢复

### lcah
不支持。

### lcah-v3
完整的检查点系统（`RuntimeCheckpointsMixin`）：

- **自动创建**：以下时机自动创建检查点
  - 文件 freshness 不匹配（`freshness_mismatch`）
  - 工作区不匹配（`workspace_mismatch`）
  - 上下文缩减（`context_reduction`）
  - 运行完成（`run_finished`）

- **检查点内容**：
  - `current_goal` / `current_blocker` / `next_step`
  - `key_files` 及其 freshness
  - `completed` / `excluded` 任务列表
  - `runtime_identity` 运行环境指纹
  - `summary` 摘要

- **Resume 状态**：启动时评估恢复状态
  - `no-checkpoint`：无检查点
  - `full-valid`：检查点完全有效
  - `partial-stale`：部分文件过期
  - `workspace-mismatch`：运行环境不匹配
  - `schema-mismatch`：检查点格式版本不兼容

- **续航引导**：当 step limit 达到时，自动生成包含当前进展的续航提示

---

## 17. Todo 待办系统

### lcah
不支持。

### lcah-v3
`TodoLedger` 提供完整的待办管理：

- 3 个工具：`todo_add`、`todo_update`、`todo_list`
- 模型可以创建、更新、查看待办事项
- Todo 变更记录到 `task_state.todo_changes`
- Todo 状态包含在 `build_report()` 输出中

---

## 18. CLI 命令行增强

### lcah
有限的 CLI 参数：
```
--cwd, --provider (ollama|openai|anthropic), --model, --host,
--base-url, --ollama-timeout, --openai-timeout, --resume,
--approval (ask|auto|never), --secret-env-name, --max-steps (default 6),
--max-new-tokens (default 512), --temperature, --top-p
```

### lcah-v3
大幅扩展的 CLI 参数：

| 参数 | 说明 |
|------|------|
| `--config` | 指定 TOML 配置文件路径 |
| `--api-key` | API Key 覆盖 |
| `--sandbox` | 沙箱模式（off/best_effort/required） |
| `--sandbox-backend` | 沙箱后端（auto/bubblewrap/none） |
| `--memory-dir` | 记忆存储目录 |
| `--no-auto-dream` | 关闭自动记忆整合 |
| `--dream-interval` | Dream 间隔（小时，默认 24） |
| `--dream-min-sessions` | 触发 Dream 的最小 session 数（默认 5） |
| `--tui` | 启动 Textual TUI |
| `--repl` | 强制使用传统 REPL |
| `--max-steps` | 默认值从 6 提升到 50 |
| `--max-new-tokens` | 默认值从 512 提升到 8192（provider 自适应） |

新增 REPL 命令（共 15+ 个）：

| 命令 | 说明 |
|------|------|
| `/help` | 查看内置命令（已增强） |
| `/skills` | 列出可用 skills |
| `/session` | 查看当前 session 路径和状态 |
| `/history` | 列出历史 session |
| `/resume <id>` | 续接 session |
| `/context` | 查看 prompt 上下文使用情况 |
| `/usage` | 查看 provider、model、token 元数据 |
| `/memory` | 查看 durable memory 索引 |
| `/working-memory` | 查看工作记忆 |
| `/remember <text>` | 记录到 daily log |
| `/dream` | 手动触发记忆整合 |
| `/plan <topic>` | 进入计划模式 |
| `/plan-exit` | 退出计划模式 |
| `/agents` | 查看子 agent 状态 |
| `/subagent <args>` | 手动启动子 agent |
| `/model <name>` | 临时切换模型 |
| `/compact` | 压缩对话历史 |
| `/clear` | 开新 session |
| `/mode` | 查看运行模式 |
| `/exit` | 退出 |

---

## 19. 测试体系

### lcah
8 个测试文件：
- test_context_manager、test_evaluator、test_memory、test_lcah、test_run_store、test_safety_invariants、test_skills、test_task_state

### lcah-v3
29 个测试文件，新增覆盖：

**验收测试**：
- `test_agent_workers_acceptance` — Worker/子 agent 功能
- `test_context_governance_acceptance` — 上下文治理
- `test_engine_acceptance` — 引擎控制循环
- `test_permissions_acceptance` — 权限系统
- `test_real_session_acceptance` — 真实会话场景
- `test_runtime_evidence_acceptance` — 运行时证据
- `test_skills_acceptance` — Skills 功能
- `test_todo_ledger_acceptance` — Todo 系统
- `test_tool_policy_acceptance` — 工具策略

**专项测试**：
- `test_architecture_boundaries` — 架构边界
- `test_ask_user` — Ask User 工具
- `test_business_scenario_dogfood` — 业务场景狗食测试
- `test_metrics` — 指标
- `test_release_smoke` — 发布烟测
- `test_run_evidence` — 运行证据
- `test_sandbox_config` / `test_sandbox_runner` — 沙箱
- `test_tui` — TUI
- `test_usage` — 用量跟踪
- `test_v3_runtime` — v3 Runtime

---

## 20. 文档体系

### lcah
单个 `README.md` (~170 行)。

### lcah-v3
完整的文档体系：

- **README.md** (~320 行)：增强版项目说明，含 TUI 截图、配置详解、命令大全
- **docs/ 目录**：专题文档
  - `configuration.md` — Provider 配置详解
  - `memory.md` — 分层记忆 + Auto-Dream
  - `skills.md` — Skills 框架
  - `sandbox.md` — 沙箱隔离

- **release/ 目录**：发布材料
  - `v3/README.md` — 发布包入口
  - `v3/CHANGELOG.md` — 变更日志
  - `v3/REVIEW.md` — 项目 pitch 与架构评审
  - `v3/TESTING.md` — 测试范围与摘要
  - `v3/testing/` — 50 个真实使用场景测试
  - `v3/learning/` — 11 篇学习文档（从整体架构到模块细节）

- **install.sh**：一键安装脚本

---

## 21. 其他差异

| 项目 | lcah | lcah-v3 |
|------|------|---------|
| 模型输出格式 | 单一 tool/final 输出 | 支持多个 tool 批量输出（`kind == "tools"`） |
| 模型错误处理 | 简单重试 | 分类错误处理，按错误码限流重试 |
| Abort 支持 | 不支持 | 支持 `abort_current_turn()` |
| Provider 重试 | 无 | 按错误码跟踪重试次数 |
| 运行事件消费者 | 无 | `runtime_consumers` 插件机制 |
| 模型客户端 | 3 个独立类 | 统一 `complete_model()` + Provider 基类 |
| 文件 freshness 跟踪 | 无 | `self_authored_file_freshness` + 文件摘要 freshness |
| Agent 自写文件跟踪 | 无 | `self_authored_file_freshness` 字典 |
| 一次性安装 | 无 | `install.sh` |
| 敏感信息处理 | 基础 redact | 增强（redact + secret key 检测 + token 模式匹配） |
| `.lcah.toml.example` | 无 | 有 |
| `.gitignore` | 基础 | 增强（含 `.lcah.toml` 排除) |

---

## 总结

lcah-v3 是 lcah 的一次全面架构升级，核心变化方向为：

1. **模块化重构**：从 15 个平铺文件拆分为 8 个子包 69 个文件，职责边界清晰
2. **可配置性**：Provider Profile、TOML 配置、多层级合并
3. **交互升级**：TUI 终端界面 + 命令行增强
4. **能力扩展**：子 Agent 管理、Plan Mode、Todo 系统、Sandbox
5. **记忆进化**：从单层工作记忆到三层持久记忆 + 自动整合
6. **工程化**：Checkpoint/恢复、事件总线、Context 压缩、验收测试体系
7. **安全性**：Sandbox 隔离、Tool Policy、write_scope、增强敏感信息处理
